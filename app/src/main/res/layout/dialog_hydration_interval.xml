<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/header_card_background">

    <TextView
        android:id="@+id/dialog_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pref_hydration_interval_dialog"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#2C3E50" />

    <TextView
        android:id="@+id/dialog_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pref_hydration_interval_instructions"
        android:textSize="13sp"
        android:textColor="#7F8C8D"
        android:layout_marginTop="8dp" />

    <EditText
        android:id="@+id/et_interval_input"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="12dp"
        android:inputType="number"
        android:background="@drawable/search_background"
        android:padding="12dp"
        android:textColor="#2C3E50"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btn_cancel_interval"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#ECEFF6"
            android:textColor="#2C3E50"
            android:text="@string/cancel" />

        <Button
            android:id="@+id/btn_save_interval"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:background="@drawable/button_filled_blue"
            android:textColor="#FFFFFF"
            android:text="@string/save" />

    </LinearLayout>

</LinearLayout>
