<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/margin_xs"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="@dimen/card_elevation"
    app:cardBackgroundColor="@color/background_light_secondary"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/padding_sm"
        android:gravity="center_vertical">

        <!-- Mood Emoji (smaller for compact layout) -->
        <TextView
            android:id="@+id/tv_mood_emoji"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="@string/emoji_happy"
            android:textSize="20sp"
            android:gravity="center"
            android:background="@drawable/mood_item_background" />

        <!-- Vertical container: top=name+rating, middle=note, bottom=timestamp -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/margin_sm"
            android:orientation="vertical">

            <!-- Top row: name on left, rating block on right -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="28dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/tv_mood_name"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:text="@string/mood_name_default"
                    android:textAppearance="@style/TextAppearance.WellnessTracker.Body"
                    android:textStyle="bold" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/margin_sm"
                    android:gravity="end|center_vertical"
                    android:orientation="vertical">

                    <!-- Small rating label is hidden to keep layout compact -->
                    <TextView
                        android:id="@+id/tv_mood_rating"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="@style/TextAppearance.WellnessTracker.Caption"
                        android:textColor="@color/text_secondary_light"
                        android:visibility="gone" />

                    <!-- Compact progress bar for visual intensity (smaller width/height) -->
                    <ProgressBar
                        android:id="@+id/pb_mood_rating"
                        style="?android:attr/progressBarStyleHorizontal"
                        android:layout_width="72dp"
                        android:layout_height="8dp"
                        android:layout_marginTop="4dp"
                        android:max="100"
                        android:progress="0"
                        android:indeterminate="false"
                        android:progressDrawable="@drawable/progress_horizontal"
                        android:progressTint="@color/primary_500" />

                    <!-- Percentage shown under the bar -->
                    <TextView
                        android:id="@+id/tv_mood_percentage"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp"
                        android:text="@string/percent_zero"
                        android:textAppearance="@style/TextAppearance.WellnessTracker.Caption"
                        android:textColor="@color/text_secondary_light" />

                </LinearLayout>

            </LinearLayout>

            <!-- Note (single-line preview to keep item compact) -->
            <TextView
                android:id="@+id/tv_mood_note"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_xs"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/mood_sample_note"
                android:textAppearance="@style/TextAppearance.WellnessTracker.Body" />

            <!-- Timestamp -->
            <TextView
                android:id="@+id/tv_mood_timestamp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_xs"
                android:text="@string/mood_sample_timestamp"
                android:textAppearance="@style/TextAppearance.WellnessTracker.Caption"
                android:textColor="@color/primary_500" />

            <TextView
                android:id="@+id/tv_mood_timestamp_raw"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:text="@string/mood_sample_timestamp_raw" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
