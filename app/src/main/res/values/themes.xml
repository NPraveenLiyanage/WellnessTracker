<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.WellnessTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WellnessTracker" parent="Base.Theme.WellnessTracker" />

    <!-- Classic splash theme using windowBackground -->
    <style name="Theme.WellnessTracker.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>